import java.awt.image.BufferedImage;

public class GoodItem extends FallingObject {

    public GoodItem(int x, int speed, int delay, int panelHeight, BufferedImage image) {
        super(x, speed, delay, panelHeight, image);
    }
}