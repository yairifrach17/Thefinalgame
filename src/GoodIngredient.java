import java.awt.image.BufferedImage;

public class GoodIngredient extends FallingObject {

    public GoodIngredient(int x, int speed, int delay, int panelHeight, BufferedImage image) {
        super(x, speed, delay, panelHeight, image);
    }
}